apiVersion: v1
kind: Service
metadata:
    name: svc-deployment-checkout
    namespace: checkout
spec:
    type: NodePort
    ports:
        - port: 80 # Porta padrão da service
          targetPort: 8080 # Porta alvo do Pod
    selector:
        app: pod-checkout